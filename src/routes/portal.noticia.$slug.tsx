import { createFileRoute, notFound } from "@tanstack/react-router";
import { PortalArticle } from "@/components/portal/PortalPages";
import { portalService } from "@/services/portalService";
import { buildSeo } from "@/lib/seo";
export const Route=createFileRoute("/portal/noticia/$slug")({loader:async({params})=>{const [post,categories,all]=await Promise.all([portalService.postBySlug(params.slug),portalService.categories(),portalService.posts()]);if(!post)throw notFound();void portalService.incrementViews(post.id);return{post,categories,related:all.filter(p=>p.id!==post.id&&p.category_id===post.category_id).slice(0,3)};},head:({loaderData})=>loaderData?buildSeo({title:loaderData.post.seo_title||loaderData.post.title,description:loaderData.post.meta_description||loaderData.post.excerpt,path:`/portal/noticia/${loaderData.post.slug}`,image:loaderData.post.social_image_url??loaderData.post.cover_image??undefined,type:"article"}):{},component:()=> <PortalArticle {...Route.useLoaderData()}/>});