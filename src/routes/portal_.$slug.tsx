import { createFileRoute, redirect } from "@tanstack/react-router";
export const Route=createFileRoute("/portal_/$slug")({beforeLoad:({params})=>{throw redirect({href:`/portal/noticia/${params.slug}`,statusCode:301});}});